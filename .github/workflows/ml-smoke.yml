name: ML pipeline smoke test

on:
  push:
    branches: [ main ]
    paths:
      - 'ml/**'
      - '.github/workflows/ml-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Compile Python sources
        run: python -m py_compile ml/*.py
